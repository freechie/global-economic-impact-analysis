# Public demo profile

`gdp_annual.csv` and `gdp_real_annual.csv` are World Bank Open Data, CC BY 4.0. The other CSVs are unused synthetic tables. The loader requires all nine tables. The notebook does not plot the synthetic tables. Their values are fictional.

```bash
make notebook
```
